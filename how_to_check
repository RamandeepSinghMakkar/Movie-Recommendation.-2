cd /Users/raman/Desktop/Projects/Movie\ Recommendation


for vercel, remove debug=True from if __name__ == "__main__":
    app.run(debug=True) in main.py
npm is necessary to download: pip install npm
downlOAD NODE.JS: https://nodejs.org/en
sudo npm install -g vercel(globally)
vercel.json is necessary (check paths carefully)
vercel login
click enter (github option is selected)
a sucess message will come
close the tab now

vercel .
select y, click enter, no, project name(lower case 
with no space) click enter
 if error comes vercel --archive=tgz
touch .vercelignore



streamlit run main.py

get recommendations for differnt user id [by default 1 is there in the code]

control c to close streamlit


mysql -u root -p
enter password
USE movie_recommendations;
SELECT COUNT(*) FROM recommendations;
SELECT * FROM recommendations WHERE user_id = 1;
DELETE FROM recommendations;
